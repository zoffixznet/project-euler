#!/usr/bin/perl

use strict;
use warnings;

use integer;
use bytes;

use CalcRects;

# use Math::BigInt lib => 'GMP', ':constant';

use List::Util qw(sum);
use List::MoreUtils qw();

STDOUT->autoflush(1);
